from django.apps import AppConfig


class CoreOrchestratorConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "core_orchestrator"
    verbose_name = "Core Orchestrator"
